{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "^/(?!api/|assets/|favicon\\.ico|robots\\.txt|manifest\\.json|sitemap\\.xml).*", "dest": "/index.html" }
  ]
}
