import { Box, Typography } from '@mui/material';

export default function GalleryManagement() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Gallery Management
      </Typography>
      <Typography color="text.secondary">Upload and manage gallery images for the public site.</Typography>
    </Box>
  );
}
